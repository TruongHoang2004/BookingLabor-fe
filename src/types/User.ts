export interface UserCard {
    id: number;
    title: string;
    description: string;
    category: string;
    location: string;
}

interface Tasker {
    id: number;
    name: string;
    expected_fee: number;
    skill: string[];
    experience: string;
}

export interface User {
    id: number;
    title: string;
    description: string;
    category: string;
    location: string;
}

