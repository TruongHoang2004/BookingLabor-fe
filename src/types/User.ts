export interface UserCard {
    id: number;
    title: string;
    description: string;
    category: string;
    location: string;
}

export interface User {
    id: number;
    title: string;
    description: string;
    category: string;
    location: string;
}
